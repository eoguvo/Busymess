{
  "name": "busymess",
  "version": "1.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "yarn sass-normalize && snowpack dev",
    "build": "yarn sass-normalize && snowpack build",
    "deploy": "npm run build && gh-pages --dist build",
    "lint": "eslint --ext .js,.mjs src/",
    "sass-normalize": "sass-migrator division node_modules/@material/**/*.scss"
  },
  "devDependencies": {
    "@snowpack/plugin-sass": "^1.4.0",
    "eslint": "^7.29.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.23.4",
    "gh-pages": "^3.2.2",
    "sass": "^1.35.1",
    "sass-migrator": "^1.5.0",
    "snowpack": "^3.5.9"
  },
  "dependencies": {
    "@material/button": "^11.0.0",
    "@material/fab": "^11.0.0",
    "@material/ripple": "^11.0.0"
  }
}
